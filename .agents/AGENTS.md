# Rules

- **Config File Backups**: Always create a backup copy of any core configuration files (e.g. `automations.yaml`, `scripts.yaml`, `config.toml`, `configuration.yaml`) before executing scripts or bulk regex updates on them.
